namespace InsideStore.Application.Results
{
    public enum ErrorType
    {
        Failure,
        BadRequest,
        Forbidden,
        NotFound,
        Conflict,
        Unauthorized,
        InternalServerError
    }
}
