namespace InsideStore.Domain.Enum
{
    public enum ErrorType
    {
        Failure,
        BadRequest,
        Forbidden,
        NotFound,
        Conflict,
        Unauthorized,
        InternalServerError
    }
}
